// Number => Integer(정수), Float(실수)
// 자바스크립트는 숫자타입을 모두 인식함.

// text는 자바스크립트가 인식할 수 없음. 따라서 큰 따옴표를 사용
// "hello"
// => String (처음부터 끝까지 text로 이루어져있는 것)

// 자바스크립트는 "" , '' 구분하지 않음. 모두 사용해도 됨.

// 상수 : 바뀌지 않는 값 constant.
const a = 5;
const b = 2;

const myName = 'yejin';
const veryLongVariableName = '낙타표기법 CamelCase';

// 파이썬은 snakeCase

console.log(a + b);
console.log(a * b);
console.log(a / b);

console.log('hello! ' + myName);

// boolean => "false, true" 처럼 string형태로 작성하지 않음.
const amIFat = false;
console.log(amIFat);
